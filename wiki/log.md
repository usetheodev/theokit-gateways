# Log

## 2026-08-06

* **Creation**: Bundle created at `wiki/` from two sources — `docs/adr/` (2 ADRs, tracked at `c891696`) and `.claude/knowledge-base/` (2 release records + 1 review record, **untracked**: `.claude/` is gitignored, so those three files were never committed). Both source directories were removed after the bundle passed `okf-validate --strict` and the creation gate.
* **Creation**: 24 concepts across `decisions/`, `core-api/`, `packages/`, `reviews/`, `releases/`, `initiatives/`, plus a root `glossary.md`.
* **Note — the bundle is now the sole copy of the knowledge-base material.** The two release records and the review record had no git history to fall back on. Their substance (verdicts, SHAs, tags, gate evidence, the peer-cascade reasoning, the honesty corrections, the security notes) was extracted into `releases/` and `reviews/` before deletion. The two ADRs remain recoverable from git at `c891696`.
* **Note — no `verified` events were written.** Every concept was produced by an agent from the sources plus the repository at `c891696`; nobody has reviewed them. Adding `verified` would misreport the trust tier.
* **Entity pass**: the five source documents named the core primitives (`MessageEvent`, `chunkText`, `chunkByGrapheme`, `GatewayConfigurationError`, `HookExecutor`) and all eleven packages without ever explaining them. Each was given its own concept, filled from the repository source at `c891696` rather than from the five documents. Terms whose whole story is one or two sentences went to `glossary.md` instead.
* **Boundary recorded**: `architect-output/ARCHITECTURE-REPORT.md` is cited by ADR-0001, ADR-0002 and the roadmap as the audit those artifacts derive from, but it is gitignored generated output and was **not** absorbed. It is referenced as provenance only. Should it be deleted, the audit's own findings are lost; what this bundle preserves is the audit's *corrected* conclusions, via the review's honesty section.
* **Boundary recorded**: two source documents referenced an npm access token pasted in-session. The incident is recorded in `releases/v1-9-0.md`, `releases/theokit-gateway-0-5-0-npm.md` and `releases/release-model.md`; **no token value or fragment was carried into the bundle.**
* **Update**: `packages/gateway/src/types/message-event.ts` and `ROADMAP.md` repointed from `docs/adr/0001-message-event-closed-union.md` to `wiki/decisions/adr-0001-message-event-closed-union.md`.
